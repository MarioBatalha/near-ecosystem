import styles from '../plusSubmit/plusSubmit.module.css';

const PlusSubmit = () => {
  return (
    <section className={styles.submitContainer}>
      <div className={styles.submitContent}>
      <h1>Submit Your Project</h1>
      </div>
    </section>
  );
};

export default PlusSubmit;
